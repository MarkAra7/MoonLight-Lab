# Base URL of the Laravel API. When omitted, requests go through the Vite dev
# proxy (vite.config.js) to http://localhost:8000.
VITE_API_BASE_URL=/api

# Base URL of the backend public storage (uploads / media).
VITE_STORAGE_BASE_URL=/storage
